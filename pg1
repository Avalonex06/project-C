#include <stdio.h>

// Fun��o que multiplica o valor da vari�vel por 2
void multiplicaPorDois(int *ptr) {
    *ptr = *ptr * 2;  // Multiplica o valor apontado por 'ptr' por 2
}

int main() {
    int var;  // Declara��o de uma vari�vel inteira

    // Solicita ao usu�rio que insira um n�mero
    printf("Digite um valor inteiro: ");
    scanf("%d", &var);  // Armazena o valor inserido na vari�vel 'var'

    // Chama a fun��o, passando o endere�o de 'var'
    multiplicaPorDois(&var);

    // Exibe o valor da vari�vel ap�s a multiplica��o
    printf("O valor de var ap�s a multiplica��o por 2: %d\n", var);
 // chorei mt pra fazer este codigo
    return 0;
}